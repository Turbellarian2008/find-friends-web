[build]
  command = "npm run build"
  publish = "dist"

[dev]
  framework = "#static"
  targetPort = 8788
  port = 8888
  publish = "site"

[build.environment]
  NODE_VERSION = "16"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
